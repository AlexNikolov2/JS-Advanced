function solve() {
    const input = document.getElementsByTagName('input')[0];
    const addButton = document.getElementsByTagName('button')[0];
    const [list1, list2, list3] = document.getElementsByTagName('ul');

    addButton.addEventListener('click', events);

    function events(){
        
    }
};